for f in *.png; do\n  w=$(sips -g pixelWidth "$f" | awk '/pixelWidth/ {print $2}')\n  echo "$w $f"\ndone | sort -n | tail -1
for f in *.png; do\n  h=$(sips -g pixelHeight "$f" | awk '/pixelHeight/ {print $2}')\n  echo "$h $f"\ndone | sort -n | tail -1
